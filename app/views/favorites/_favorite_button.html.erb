<% if logged_in? %>
  <% if current_user.like?(post) %>
    <%= form_with(model: current_user.favorites.find_by(post_id: post.id), local: true, method: :delete) do |f| %>
      <%= hidden_field_tag :post_id, post.id %>
      <%= button_tag :type=> 'submit', class: 'btn btn-danger' do %><i class="fas fa-heart"></i><% end %>
    <% end %>
  <% else %>
    <%= form_with(model: current_user.favorites.build, local: true) do |f| %>
      <%= hidden_field_tag :post_id, post.id %>
      <%= button_tag :type=> 'submit', class: 'btn btn-primary' do %><i class="far fa-heart"></i><% end %>
    <% end %>
  <% end %>
<% end %>