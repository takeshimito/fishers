<div class="index">
  <div class="headder">
    <h5>⭐️️投稿ランキング⭐️</h5>
    <p>お気に入り（ハート）の数を参考に多い順に並べております。</p>
  </div>
  <% @posts.each do |post| %>
    <div class="card-sm my-3 each-card" style="border ">
      <div class="row no-gutters">
        <div class="col-md-4 img-box">
          <%= image_tag post.image.url, class: "post-img mx-auto" if post.image? %>
        </div>
        <div class="col-md-8">
          <div class="card-body">
            <h5 class="card-title">
              <%= link_to post.user.name, user_path(post.user) %>:
              <%= link_to post.title, post_path(post) %>
            </h5>
            <p class="card-text">
              <%=safe_join(post.content.split("\n"),tag(:br))%>
            </p>
            <p class="card-text">
              <small class="text-muted">
                緯度：<%= post.latitude %>
                経度：<%= post.longitude %>
                <%= post.prefecture.name %>
              </small>
            </p>
            <%= render 'favorites/favorite_button',  post: post %>
            <div>お気に入り：<span class="badge badge-secondary"><%= post.favorite_users.count %></span></div>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>